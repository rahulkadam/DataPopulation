# The following environment variables are set by a shell/batch
# script on the environment for which the build will be run
# 
# JDK_HOME
# ANT_HOME
# DOC_HOME



#build directories
build.root = ../../
app.name = jackson
app.root  = ${build.root}/${app.name}
build.dir  = ${app.root}/build
log.dir  = ${app.root}/log
deployment.dir =${app.root}/deployment
src.dir = ${app.root}/src
lib.dir = ${app.root}/lib
extlib.dir = ${app.root}/extlib
srcdeployment.dir = ${app.root}/deployment/src
unear.keyreplacement = true
earfilename=provgui.ear

#jdk, weblogic and ant related
java.bin = /usr/bin/java
javac.cmd = /opt/jdk1.7.0_71/bin/java
jvm.cmd = ${java.bin}/java
ant.dir = ${env.ANT_HOME}

 
# configurable test environment


pmd.home=${env.PMD_HOME}
pmd.reports.dir=${build.dir}/reviewreports
findbugs.home=${env.FINDBUG_HOME}
findbug.reports.dir=${build.dir}/reviewreports
temp.pmd.report=${build.dir}/pmdreports

checkstyle.home=${env.CHECKSTYLE_HOME}
checkstyle.reports.dir=${build.dir}/checkstylereports



